subprojects {
    buildscript {
        repositories {
            jcenter()
        }
    }

    repositories {
        mavenLocal()
        jcenter()
    }

    group = GROUP
    version = VERSION_NAME
}

apply from: rootProject.file('dependencies.gradle')